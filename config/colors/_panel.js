import * as COLORS from './DEFAULT';

export const PANEL = {
  primary: {
    base: `${COLORS.PRIMARY}.50`,
    border: `${COLORS.PRIMARY}.400`,
  },
  secondary: {
    base: `${COLORS.SECONDARY}.50`,
    border: `${COLORS.SECONDARY}.400`,
  },
  transparent: {
    base: `transparent`,
    border: `${COLORS.PRIMARY}.400`,
  },
};
